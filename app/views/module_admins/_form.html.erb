<%= form_for(module_admin) do |f| %>
  <% if module_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(module_admin.errors.count, "error") %> prohibited this module_admin from being saved:</h2>

      <ul>
      <% module_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :surname %>
    <%= f.text_field :surname %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
